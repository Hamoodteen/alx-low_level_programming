#include "hash_tables.h"

/**
 * key_index - f
 * @key: key
 * @size: size
 * Return: int
*/
unsigned long int key_index(const unsigned char *key, unsigned long int size)
{
	(void)key;
	(void)size;
	return (0);
}
